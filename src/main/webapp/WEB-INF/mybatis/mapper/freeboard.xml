<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="freeboard">

	<insert id="insertOne" parameterType="freeboard">
		INSERT INTO FREEBOARD	(NO,WRITER,CATEGORY,TITLE,CONTENT,WRITEDATE)
		VALUES (EMP_SEQ.NEXTVAL,#{writer},#{category},#{title},#{content},SYSDATE)
	</insert>

	<select id="selectAll" resultType="freeboard">
		SELECT * FROM FREEBOARD ORDER BY NO 
	</select>
	
	<select id="selectOne" resultType="freeboard" parameterType="int">
		SELECT * FROM FREEBOARD WHERE NO = #{no}
	</select>


</mapper>